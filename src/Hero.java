public class Hero extends AnimatedThing {
    public Hero(String fileName, double x, double y, int attitude, int index, int maxIndex) {
        super(fileName, x, y, attitude, index, maxIndex);
    }

}
