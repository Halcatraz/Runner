public class Hero extends AnimatedThing {

    public Hero(double x, double y, int indexMax, String state, String lastState, String fileName) {
        super(x, y, indexMax, state, lastState, fileName);
    }
}
